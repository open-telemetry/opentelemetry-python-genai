# CrewAI caps the OpenTelemetry API, SDK, and OTLP HTTP exporter below the
# workspace's supported ~=1.43 line. Relax those transitive caps so the
# instrumentation package can be tested with the repository's OTel stack.
opentelemetry-api ~= 1.43
opentelemetry-sdk ~= 1.43
opentelemetry-exporter-otlp-proto-http ~= 1.43
