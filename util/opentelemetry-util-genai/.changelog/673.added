Add ``suspend()`` and ``activate()`` to GenAI invocations for handing context back to the caller while the invocation is still running, and ``SyncToolStreamWrapper``/``AsyncToolStreamWrapper`` for tool executions returning a stream.
