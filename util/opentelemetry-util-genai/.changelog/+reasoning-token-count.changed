`gen_ai.usage.output_tokens` is now taken directly from `invocation.output_tokens` and no longer auto-added to `thinking_tokens`. 
Instrumentations must include reasoning/thinking tokens in `output_tokens` when their underlying library API does not do it.
